# Complex Continued Fraction
A means to visualize the norm of partial denominators of complex continued
fractions.

## Development
This project uses has an initial exploration in [Elm][elm].
Unfortunately the precision of `Int` is quickly reached.

That is why we are looking into a [Rust][rust] implementation.

[elm]: https://elm-lang.org/
[rust]: https://rust-lang.org/
